import { ListTreeUseCase } from './ListTreeUseCase';

let listTreeUseCase: ListTreeUseCase;

describe('List Tree', () => {
  beforeEach(() => {
    listTreeUseCase = new ListTreeUseCase();
  });

  it('should be able to list the tree', () => {
    // listTreeUseCase.execute();
  });
});
